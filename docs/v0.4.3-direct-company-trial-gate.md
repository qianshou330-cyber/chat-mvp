# v0.4.3-beta 直接受控公司试用闸门

## 基线

- 生产站点：`https://chat-mvp-tau.vercel.app`
- 当前试用基线：`v0.4.3-beta`
- tag 指向：`02738f8`
- 当前 `main`：`2dce3e9`
- 验收时间：2026-06-18
- 临时账号前缀：`codex.pilot.direct.`

## 自动 smoke 结果

- 本地检查通过：`npm run lint`、`npm run test -- --run`、`npm run build`。
- 生产站点 `/`、`/manifest.webmanifest`、`/sw.js` 均返回 200。
- 5 个临时账号完成注册/登录：owner、admin、member1、member2、member3。
- owner 默认工作区创建成功，并添加 1 名 admin、3 名 member。
- owner 可调整成员角色；member 不能执行成员管理。
- owner/admin 可读取管理员记录；member 读取不到管理员记录。
- owner 与 member1 完成好友申请、同意、direct chat 消息。
- 非 direct 成员不能读取 A/B 私聊消息。
- `create_group_conversation` 创建工作区群聊成功，群成员加入成功。
- 群消息、中文关键词数据查询、附件上传和 signed URL 访问成功。
- member3 被移除工作区后，不能继续读取工作区群聊消息和附件。
- member3 已重新加入工作区和群聊，验证完整成员环境可恢复。
- 登录设备记录、退出其他设备路径通过。
- push subscription RLS 通过，其他用户不能读取订阅。

## Chrome 与通知补验

- Chrome 生产页面可登录临时 admin 账号。
- 个人资料页显示：工作区管理、反馈问题、试用说明、管理员记录、登录设备、消息通知。
- 点击“开启通知”后，页面显示“消息通知已开启”。
- owner 发送消息后，真实 Chrome push subscription 的 `last_sent_at` 更新，`last_error = null`。
- 通知 payload 继续保持隐私策略：不包含消息正文、附件名或邮箱。

## 清理结果

- 已删除 10 个 `codex.pilot.direct.*@example.com` 临时 Auth 用户。
- 已删除 2 个 `codex-pilot-direct-*.txt` 测试附件。
- SQL Editor 复查结果：
  - `auth.users` 命中数：0
  - `storage.objects` 命中数：0

## 结论

`v0.4.3-beta` 已通过“跳过 5 人先导，直接进入 20-30 人受控公司试用”前的强化自动验收。若真实试用出现 P0/P1，进入 `v0.4.4-beta` 最小稳定性修复；P2/P3 先记录到 GitHub Issues，不打断试用。
