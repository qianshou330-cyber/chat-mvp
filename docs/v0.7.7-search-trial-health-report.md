# v0.7.7-beta Search Trial Health Report

`v0.7.7-beta` is a production-health and trial-feedback closure patch for the v0.7 search series. It keeps the existing Supabase RPC search approach and does not add channels, bots, voice/video calls, end-to-end encryption, or a dedicated search service.

## Feedback Status

- GitHub Issues were checked for `search`, `search UX`, `filter`, and the matching Chinese terms. The only matches were historical planning or external-test tracking issues; no current v0.7 search-specific P0/P1 issue was found.
- No real user search feedback has been received yet. This report relies on production smoke and sanitized production health checks, not real user conclusions.
- Any future P0/P1 search issue should block the next search patch and require a minimal fix plus repeated smoke.

## Production Health Checks

- App error events: SQL Editor read-only check for the last 7 days returned 0 search-related rows in `public.app_error_events`.
- Production smoke: passed with temporary `codex.v077.*@example.com` A/B/C accounts.
- Temporary data cleanup: completed. `codex.v077.*@example.com` Auth users returned to 0, and `codex-v077-*` Storage objects returned to 0.
- GitHub Issues check: no current search P0/P1 was found.

## Smoke Scope

- A/B/C accounts created a production group, added a member, sent Chinese keyword text messages, and uploaded a test image.
- Current-conversation search found the Chinese keyword, showed the completed-results state, and could open a result then return with the search state preserved.
- Global search found the Chinese keyword, showed load-more/completed state correctly, and kept search state after returning from a result.
- Empty-result behavior stayed inside the search area.
- Image type-filter smoke was checked with the production search UI.
- C, as a non-member, could not see or search A/B restricted private or group content.

## Sanitization Notes

- The health report does not store raw search keywords, message bodies, attachment names, tokens, passwords, webhook secrets, VAPID private keys, or service-role keys.
- Storage cleanup was performed through the Supabase Storage UI rather than direct SQL deletion of `storage.objects`.
- Auth cleanup used an email-prefix-limited SQL statement targeting only `codex.v077.%@example.com`.

## Boundaries

- Keep v0.7.7 as a search production-health patch.
- Do not add new Supabase tables, RPCs, RLS policies, Edge Functions, or search infrastructure unless production smoke exposes a confirmed P0/P1 security or reliability gap.
