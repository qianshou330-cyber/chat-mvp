# v0.6.1-beta 生产 Smoke 与收口记录

## 基线

- 日期：2026-06-19
- 生产地址：`https://chat-mvp-tau.vercel.app`
- 功能基线：`v0.6.0-beta`
- 目标标签：`v0.6.1-beta`
- 范围：运维收口、文档同步、测试数据清理、真实 Chrome Web Push smoke。

## 本地检查

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| `npm run lint` | 通过 | 2026-06-19 运行通过。 |
| `npm run test -- --run` | 通过 | 2 个测试文件，28 个测试通过。 |
| `npm run build` | 通过 | 主 bundle 约 514 kB，仍有大 chunk 和 `inlineDynamicImports` 弃用警告；拆包或配置优化放到 v0.6.2。 |

## 生产 smoke

| 路径 | 状态 | 备注 |
| --- | --- | --- |
| Vercel Production 最新 bundle | 通过 | 生产 HTML 加载 `assets/index-3JK0wCbX.js` 和 `assets/index-Bj_CQE2-.css`；bundle 包含“试用巡检”“邀请说明”和群禁言文案。 |
| PWA manifest / service worker | 通过 | `manifest.webmanifest` 和 `sw.js` 均返回 200。 |
| 登录、退出、重新登录 | 通过 | Chrome 使用 `codex.v060.push.*` 临时账号切换 owner/member 成功。 |
| 群邀请说明 | 通过 | owner 群详情页可见“邀请说明”和“复制邀请文案”。 |
| 试用巡检 | 通过 | owner 群详情页“更多管理”可见试用巡检，最近错误/通知失败/附件失败均为 0，管理操作为 1。 |
| 群禁言权限 | 通过 | owner 开启全体禁言后，member 看到禁言原因，附件、输入框和发送按钮均禁用。 |
| 图片/视频/文件 | 未覆盖 | v0.6.1 未新增媒体逻辑；本轮只复核文字消息、禁言和通知链路。完整媒体回归沿用 `docs/deployment-checklist.md`。 |
| 登录设备 | 通过 | member 个人资料页显示“1 台设备正在使用这个账号”。 |
| Web Push 真实通知 | 通过 | Chrome 端通知已开启；A 发消息后 B 的 `push_subscriptions.last_sent_at` 更新且 `last_error` 为空。自动化无法读取 Windows 系统通知浮层，服务端投递记录作为本轮凭据。 |

## 权限回归

| 路径 | 状态 | 备注 |
| --- | --- | --- |
| member 看不到管理入口和管理记录 | 通过 | member 群详情只显示只读群信息、公告/权限、群文件和成员列表。 |
| 非成员不能读私聊、群消息、附件、群文件 | 通过 | outsider 查询本轮群消息和成员列表返回 0 行。 |
| 非成员不能读 push subscription 或管理记录 | 通过 | outsider 查询其它 push subscription、`admin_activity_logs` 和 `app_error_events` 均返回 0 行。 |
| 被移出群成员刷新后不能继续访问群内容 | 未覆盖 | 本轮不做成员移除副作用，避免扩大临时数据；该项仍保留在正式扩容 smoke。 |

## 测试数据清理

| 对象 | 状态 | 备注 |
| --- | --- | --- |
| `codex.v060.*@example.com` Auth 用户 | 通过 | 清理前预览命中 6 个临时账号；删除后复验为 0。 |
| `codex-v060-` Storage 测试附件 | 通过 | 清理前预览为 0；删除后复验仍为 0。本轮未上传新测试附件。 |

## 结论

本地检查、生产只读检查、Chrome Web Push smoke、群邀请说明、试用巡检、群禁言、非成员 RLS 回归和 `codex.v060.*` 测试数据清理均已通过。未发现 P0/P1，可创建并推送 `v0.6.1-beta`。
