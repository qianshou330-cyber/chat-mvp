# v0.4.2-beta 稳定性补丁

## 目标

`v0.4.2-beta` 面向 5 人先导试用后的第一轮小修，只处理稳定性、权限、通知、附件、成员管理和移动端阻塞问题，不扩张新功能。

## 本轮补丁范围

- 消息或附件发送失败后，清理本地“发送中”状态，避免测试者误以为仍在排队。
- 通知开启/关闭失败时，显示中文可理解原因，并继续写入脱敏错误记录。
- 通知状态以当前用户已保存的有效订阅为准，避免同一浏览器切换账号后误显示已开启。
- 工作区成员移除或角色更新成功后，立即刷新本地列表和角色状态。
- 工作区成员管理操作中禁用重复点击，降低重复提交风险。
- 添加成员表单明确提示：对方需要先完成注册，才能加入工作区。
- 管理员记录和关键错误记录增加更明确的空状态说明。

## 不做

- 不做视频动态头像。
- 不做频道和机器人。
- 不做语音/视频通话。
- 不做端到端加密。
- 不改核心数据模型，除非后续发现权限或数据一致性 P0/P1。

## 验收清单

- 本地通过 `npm run lint`、`npm run test -- --run`、`npm run build`。
- 生产 smoke 覆盖登录、工作区、成员管理、好友申请、单聊、群聊、附件、搜索、通知、登录设备和管理员记录。
- 非成员不能读取私聊、工作区群聊、附件、push subscription 或管理员记录。
- 被移除成员刷新后不能继续访问工作区群聊和附件。

## 打 tag 闸门

- Vercel Production 必须已经加载包含本补丁的新 bundle。
- `manifest.webmanifest` 和 `sw.js` 必须可访问。
- 未登录 anon 客户端不能读取 `messages`、`attachments`、`push_subscriptions`、`admin_activity_logs` 或 `workspace_members`。
- `send-message-push` 在未带 webhook secret 时必须返回 401。
- 真实 A/B/C 账号 smoke 无 P0/P1 后，才创建并推送 `v0.4.2-beta`。

## 生产 smoke 结果

- 本地 `lint/test/build` 已通过。
- Vercel Production 已加载 v0.4.2 补丁 bundle。
- A/B/C 临时账号 smoke 已通过：工作区、成员管理、好友申请、单聊、群聊、附件、搜索、权限回归均正常。
- Chrome 通知 smoke 已通过：同一浏览器切换账号不再误判通知状态；重新开启通知后写入订阅，消息触发后 `last_sent_at` 更新且 `last_error` 为空。
- 临时账号使用 `codex.v042.*@example.com`，后续按 `docs/test-data-cleanup-sop.md` 在 Supabase Dashboard 清理 Auth 用户和测试附件。
